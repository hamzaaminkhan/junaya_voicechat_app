import 'package:flutter/material.dart';

class ChangeGmailScreen extends StatelessWidget {
  const ChangeGmailScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Change Gmail')),
      body: const Center(child: Text('Change Gmail')),
    );
  }
}

