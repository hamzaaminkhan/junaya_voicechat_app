class AppConfig {
  AppConfig._();

  // REST API → Node.js port 5000
  static const String apiBaseUrl =
      'https://structural-among-visits-image.trycloudflare.com';

  // Socket server → Node.js/socket port 3000
  static const String socketBaseUrl =
      'https://altered-compatible-aug-kidney.trycloudflare.com';
}